# Strict by default. A package may only import what it declares (ADR-0014).
auto-install-peers=false
strict-peer-dependencies=true
resolution-mode=highest

# Never silently install a different version than the lockfile says.
prefer-frozen-lockfile=true

# Keep hoisting off so undeclared imports fail locally, not in CI.
shamefully-hoist=false
